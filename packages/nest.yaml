script:
  nest_cast_kitchen:
    alias: "Casta i köket"
    sequence:
      - data:
          dashboard_path: lovelace
          entity_id: media_player.nest_kok
          view_path: kok
        service: cast.show_lovelace_view

google_assistant:
  project_id: lcv4-home-assistant
  service_account:
    private_key: !secret google_assistant_service_account_private_key
    client_email: !secret google_assistant_service_account_client_email
  report_state: true
  exposed_domains:
    #- switch
    - light
    #- sensor
  entity_config:
    light.timeruttag_rela:
      aliases:
        - timeruttag
        - uttagen
        - timerbelysning
    light.halo_dimmer:
      expose: false
    light.halo_downlight:
      expose: false
  #     expose: false
  #     aliases:
  #       - ringklocka
  #     room:
  #       - hall
